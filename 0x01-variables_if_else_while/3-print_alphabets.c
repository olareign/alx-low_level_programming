#include <stdio.h>
#include <stdlib.h>
/**
 * main -print an alphabets in lowercase then in uppercase
 * Return: 0
*/
int main(voi)
{
	int char = "a";
	int char = "A";

	while (a <= 'z')
	{
		putchar(ch);
		ch++;
	}
	while (a <= 'Z')
	{
		puchar(CH);
		CH++;
	}
}

